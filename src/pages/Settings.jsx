const Settings = () => {
	return (
		<section>
			<h1 className="text-2xl font-semibold">Settings</h1>
			<p className="text-gray-500 mt-2">Update profile, preferences, and app configuration.</p>
		</section>
	);
};

export default Settings;
